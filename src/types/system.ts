export interface SystemStep {
  id: string;
  title: string;
  summary: string;
  badge: string;
}

export interface ExperienceEntry {
  id: string;
  title: string;
  company: string;
  range: string;
  impact: string;
  highlights: string[];
}
